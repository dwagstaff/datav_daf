package com.bsquare.datav.trucksidetool.re;


// Raise score of steps associated with active DTCs
rule "Raise score of active DTC"
    when
        DTC( $dtc: symptomId, active == EYesNo.Yes )
        $ps: PossibleStep(dtc == $dtc, $stepId: stepId )
    then
        insert( new AdjustScore($stepId, 5.0, "Increase score of active DTC") );
end
        